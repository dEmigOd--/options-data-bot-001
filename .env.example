# Copy to .env and fill in. Never commit .env.

# IBKR (read-only; TWS or Gateway must be running)
# IBKR_USERNAME=
# IBKR_PASSWORD=
# IBKR_HOST=127.0.0.1
# IBKR_PORT=7497

# SQL Server: one database (any name); tables inside are per ticker (e.g. option_snapshots_SPX).
# Create the database and a SQL login, then use UID/PWD (recommended):
# SQL_CONNECTION_STRING=DRIVER={ODBC Driver 17 for SQL Server};SERVER=localhost\SQLEXPRESS;DATABASE=SPXOptions;UID=spxbot;PWD=your_password;
# Or Windows auth (your Windows user must have access to the database):
# SQL_CONNECTION_STRING=DRIVER={ODBC Driver 17 for SQL Server};SERVER=localhost\SQLEXPRESS;DATABASE=SPXOptions;Trusted_Connection=yes;
