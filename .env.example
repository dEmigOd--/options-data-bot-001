# Copy to .env and fill in. Never commit .env.

# IBKR (read-only; TWS or Gateway must be running)
# IBKR_USERNAME=
# IBKR_PASSWORD=
# IBKR_HOST=127.0.0.1
# IBKR_PORT=7497

# SQL Server: default database name OptionData (or set SQL_DATABASE). App creates DB if missing.
# Create a SQL login and use UID/PWD (recommended):
# SQL_CONNECTION_STRING=DRIVER={ODBC Driver 17 for SQL Server};SERVER=localhost\SQLEXPRESS;DATABASE=OptionData;UID=spxbot;PWD=your_password;
# Or Windows auth:
# SQL_CONNECTION_STRING=DRIVER={ODBC Driver 17 for SQL Server};SERVER=localhost\SQLEXPRESS;DATABASE=OptionData;Trusted_Connection=yes;
# SQL_DATABASE=OptionData
